import { styled } from "@mui/material";

export const FloatingBadge = styled('div')({
    position: 'absolute',
    backgroundColor: '#FFA500',
    color: '#FFFFFF',
    borderRadius: '4px',
    padding: '8px',
    fontSize: '12px'
});